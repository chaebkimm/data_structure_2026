# Module 6 — Stage E: Lab and Evidence

Start with `student/lab.md`, then edit only:

- `code/starter/graph_dfs.c`;
- `code/tests/test_student.c`.

The supplied adjacency-matrix graph and vertex-ID Stack are retrieval
infrastructure from earlier modules. Students implement recursive DFS,
iterative DFS, and undirected connected-component counting. The autopsy—a
careful investigation after a failure—uses a bounded trace of an intentional
cycle-handling defect, so it cannot run forever.

Submit the files and evidence listed in the lab, then complete
`student/evidence_template.md`.
