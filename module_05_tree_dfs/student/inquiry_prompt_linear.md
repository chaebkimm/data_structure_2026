# Stage A — Initial Inquiry: Linear Accessible Format

## How to use this version

This version has the same learning targets as the standard inquiry. It avoids
drawing requirements, response tables, and long blank lines. It is suitable
for a **screen reader**—software that reads on-screen text aloud—keyboard
navigation, speech input, or a response containing only text.

Use only this file during the initial inquiry. Preserve your first answers.
Later, add a labeled correction instead of replacing an initial answer.

This activity reuses tree words from Module 2. A **tree** is a hierarchy of
connected items. Its one starting item is the **root**. Each item is a
**node**. A node may have a **left child** and a **right child** below it. A
**leaf** is a node with no children.

The example is a synthetic policy tree. **Synthetic** means invented for
safe practice. A **policy** is a set of rules. A computer **directory**
organizes entries so stored items can be located. This is not a real
directory or security policy.

## Student information

Name:

Date:

## A. Read the tree

Use these relationships:

1. Root key 50 has left child 30 and right child 70.
2. Key 30 has left child 20 and right child 40.
3. Keys 20, 40, and 70 have no children.
4. Keys 30, 40, and 70 carry review markers.

A **review marker** is a stored yes-or-no note saying that an item needs
attention.

### A1. All keys

List every key in the tree.

Response:

### A2. Root

Which key is the root?

Response:

### A3. Leaves

Which keys are leaves?

Response:

### A4. Marked keys

Which keys have review markers?

Response:

## B. Invent a visiting rule

To **visit** a node means to stop at it and perform the current task. In this
activity, the task is to write its key.

### B1. Repeatable rule

Invent a repeatable rule that starts at the root, visits every node exactly
once, and prefers a left choice before a right choice.

Response:

### B2. Visit sequence

Use your rule on the tree. State the keys in the order visited.

Response:

### B3. Completeness

How will your method know that no node was missed?

Response:

## C. Remember choices not yet taken

Suppose your method begins at key 50, then reaches key 30, then reaches leaf
20.

### C1. Remaining work

At key 20, which parts of the tree still need attention?

Response:

### C2. Remembered information

What information must be remembered so the method can return to those parts?

Response:

### C3. Nested unfinished work

Could one remembered item be completed while another older item is still
unfinished? Explain.

Response:

## D. Report selected nodes

Suppose the task is to report only nodes carrying a review marker. The method
must still inspect enough of the tree to find every marked node.

### D1. Report

Which keys should be reported?

Response:

### D2. Same relationships

Would changing what gets reported require changing the tree’s child
relationships? Explain.

Response:

## E. Consider stopping conditions

### E1. Missing child

What should your method do when it reaches a missing child position?

Response:

### E2. Leaf

What should it do after visiting a leaf?

Response:

### E3. Safe stopping

What would go wrong if the method kept following missing child positions
forever?

Response:

## F. Macro-Question

How can one repeatable method visit every node in a tree, remember unfinished
choices, and stop safely?

Initial explanation:

## G. Open question

What question does your current method not settle?

Response:
