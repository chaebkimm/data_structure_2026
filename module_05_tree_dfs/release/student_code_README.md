# Student Code Package

Edit only `starter/tree_dfs.c` and `tests/test_student.c`.

```sh
make starter-core
make starter-student-tests
make autopsy
```

The starter compiles but fails until its three TODO operations are complete.
The public grammar accepts single digits, `+`, and `*`. The node pool holds
100 nodes.
