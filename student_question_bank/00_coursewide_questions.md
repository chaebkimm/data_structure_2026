# Course-Wide Student Question Bank

> Scope: This bank catalogs distinct, useful question intents that can arise across the course. It is comprehensive by topic, but it does not repeat every possible wording of the same question. It contains questions only, not answers.

## Course Structure and the 14-Week Map

- Why are there 16 numbered teaching packages but only 14 instructional weeks?
- Does a module number always match the week in which we use it?
- Which document controls if a package README and the 14-week delivery guide appear to disagree?
- Where can I find the definitive list of required work for the current week?
- Why are Modules 8 and 9 combined into one BFS week?
- Why are Modules 10 and 11 combined into one Priority Queue and Heap week?
- Why is Hash Table numbered Module 13 instead of appearing inside one of the five spirals?
- What does the Linear-to-Tree-to-Graph spiral organization mean for how I should study?
- Are concepts introduced as previews expected knowledge when they return later?
- Do I need to complete the packages strictly in numerical order?
- Which weeks use two packages within the same 180-minute contact-time budget?
- What does the 180-minute weekly cap include?
- Is the capstone defense part of the 14 teaching weeks?
- Which weeks replace an ordinary lab with a practical?
- When does the capstone begin, and why is it not introduced for the first time in Week 14?
- What prior C knowledge does the course assume, and where can I get help if my background is different?

## Required Work, Extensions, and Staged Releases

- How can I tell whether a file, activity, or implementation is required, supplied, or extension-only?
- Does extension-only work affect my grade if I choose not to do it?
- Can an extension become an unannounced prerequisite for later required work?
- May I submit extension work for feedback even when it carries no required credit?
- Can extension work be included in my portfolio?
- What do the staged release labels mean?
- What must I demonstrate before the next release stage becomes available?
- May I work ahead through release gates if I have completed the current evidence?
- Why does a full package contain more material than the current week requires?
- How do I distinguish a week-specific curated release from a complete staged package?
- What should I do if the release manifest and the files I received do not match?
- Am I expected to implement code that the delivery guide says will be supplied as a baseline or scaffold?
- May I modify supplied baseline code, or must I treat it as a fixed reference?
- When are instructor tests, solutions, or later-stage files released?
- If I open optional material, does that make it required or eligible for assessment?

## Weekly Submissions, Deadlines, and Workload

- Is there ever more than one required programming submission in a teaching week?
- What belongs in the single combined Week 8 BFS submission?
- What belongs in the single combined Week 9 Priority Queue and Heap submission?
- Why is there no separate ordinary lab submission in Weeks 6 and 10?
- How is the Week 14 MST, synthesis, and capstone-integration work folded into one artifact?
- Does each capstone milestone require a second weekly submission?
- What files and evidence must be included in an ordinary weekly lab artifact?
- Where are due dates and late-work rules published?
- What should I do if the submitted archive is corrupt or omits a required file?
- How should I submit work that builds correctly on my computer but not in the course environment?
- May I replace a required artifact with a harder extension implementation?
- How should I label optional files so they are not mistaken for required work?
- What should I do if the bounded weekly core is taking substantially longer than expected?
- Does one combined submission receive one due date and one rubric even though it draws from two packages?

## Grading, Feedback, Revisions, and Embedded Checks

- How is the final course grade divided among retrieval work, labs, synthesis checks, debugging artifacts, practicals, the portfolio, and the capstone?
- How are the common lab-rubric criteria applied to a repair or trace rather than a full implementation?
- Can correct output earn full credit if I cannot explain the representation or invariant?
- How much credit depends on memory safety even when the program appears to work?
- How are student-authored tests and tool-supported evidence graded?
- How is complexity analysis graded when measured performance differs from the asymptotic result?
- What counts as a meaningful cybersecurity transfer explanation?
- Are Cognitive Pauses graded for correctness, completion, revision, or participation?
- How do the two dropped retrieval or Cognitive Pause scores work?
- Which teaching-week labs permit a feedback-driven revision?
- What must change for a revision to demonstrate new learning rather than simple resubmission?
- How much credit can an expert-model correction recover on a practical?
- Where are the five spiral synthesis checks graded if they are not separate submissions?
- Are Segfault Autopsies and Micro-CTFs additional projects or embedded parts of existing work?
- How is the same code prevented from being scored twice in the Week 14 lab and capstone grades?

## Practical Assessments

- What skills and earlier topics are cumulative on Practical 1?
- What skills and earlier topics are cumulative on Practical 2?
- What is the balance among tracing, implementation, debugging, repair, selection, and explanation on a practical?
- Which starter code and data-structure libraries are supplied during each practical?
- Are notes, package materials, a debugger, compiler warnings, or sanitizers allowed during a practical?
- How much time is available for each practical?
- How are partial implementations and correct diagnostic reasoning scored?
- Are hidden tests used on practical code, and what kinds of contracts may they check?
- What happens if my code does not compile at the end of a practical?
- What is the make-up process for an excused absence during a practical?
- What accessibility or extended-time arrangements are available for practicals?
- Why does each practical replace the ordinary lab for its week instead of adding to it?

## Portfolio, Capstone, and Defense

- What is a data-structure passport entry, and how concise should each entry be?
- Which artifacts count as a substantial revision for the final portfolio?
- How should I show that a misconception was corrected?
- What belongs in each Linear-to-Tree-to-Graph connection map?
- Which test, sanitizer, and Segfault Autopsy evidence should I preserve during the semester?
- May I submit the portfolio as Markdown, accessible HTML, or another approved format?
- What accessibility requirements apply if I choose PDF for the portfolio?
- Which capstone capabilities must be present in the final Defensive Network Analysis Toolkit?
- How should I carry each weekly capstone milestone forward without creating incompatible copies?
- May the capstone reuse tested course libraries and instructor-supplied scaffolds?
- Which capstone decisions must be justified rather than merely implemented?
- What does it mean to cross-check Prim and Kruskal totals when both are enabled?
- How should a pair divide capstone work while preserving shared understanding?
- What individual evidence must each partner submit?
- What kinds of questions or live modifications can appear in the defense?
- Can the defense be written, recorded, extended-time, or otherwise adapted?
- What happens if a partner withdraws, becomes unavailable, or contributes unevenly?
- When and where does the defense occur if the institution has no separate final-assessment period?
- How are integration, the report, and the individual defense distinguished from the Week 14 lab grade?

## Attendance, Make-Up Work, Accessibility, and Psychological Safety

- What should I do when I miss a lecture, lab meeting, or Cognitive Pause?
- What asynchronous equivalent is available for an absence?
- What is the normal make-up window, and how is it requested?
- How can I recover the required bridge material if I miss a combined week?
- May I use a drawing, structured table, tactile model, or verbal description for a memory diagram?
- May I complete Cognitive Pauses on paper, in an accessible editor, or with speech-to-text?
- Are commands, live-coded examples, captions, and diagram descriptions available in text-accessible form?
- Is any task graded on color perception, handwriting quality, typing speed, or visual polish?
- What alternatives are available when a physical manipulative or embodied activity is inaccessible?
- How can I request screen-reader-friendly starter code or another accessible file format?
- How can I arrange extended time without being penalized for coding or speaking speed?
- Must I disclose a diagnosis to ask about an access barrier?
- How can I report that an activity, example, or classroom interaction feels unsafe or exclusionary?
- Can I ask for help without being publicly identified as behind or assigned a fixed ability label?
- How is prior programming experience considered without treating it as permanent ability?
- Where can I give anonymous feedback about workload, accessibility, cybersecurity relevance, or psychological safety?

## Pair Work, Help, Sources, and Academic Integrity

- Which assignments permit pair work, and which must be completed individually?
- What may partners share on the capstone, and what must each partner produce independently?
- May I discuss diagrams, invariants, pseudocode, or test ideas with classmates on an individual lab?
- When does collaborative debugging become unauthorized code sharing?
- May I show a compiler error or debugger trace to a classmate without sharing my solution?
- May I reuse code I wrote in an earlier course module?
- May I use instructor-supplied code in later assignments without citing every copied line?
- How should I cite production excerpts, documentation, tutorials, or external code?
- Are third-party libraries allowed when the assignment is intended to assess a data-structure implementation?
- What are the rules for using generative AI or code-completion tools?
- If an automated tool suggests code, what understanding and attribution am I responsible for?
- May I compare my program's output with another student's without viewing their source?
- May I share student-authored tests, fuzz seeds, or failing inputs?
- What should I do if I accidentally receive a solution or later-stage release?
- How should contribution history be documented when a pair uses version control?
- What happens if two submissions are similar because both follow the same supplied scaffold?

## Toolchain, Building, Warnings, Sanitizers, and Debugging

- Which compiler versions and operating systems are officially supported?
- What does C11-compatible mean for the code I submit?
- What compiler-warning flags should I enable for every build?
- Are warnings treated as errors in the grading environment?
- How do I distinguish a compiler warning from a linker error or runtime failure?
- Which directory should I run the build command from?
- When should I use `make`, and what should I do if `make` is not installed?
- What PowerShell command should I use for the equivalent build on Windows?
- What should I do if PowerShell execution policy blocks a provided script?
- How can I handle workspace paths that contain spaces?
- How do I perform a clean rebuild so stale object files do not hide a problem?
- Which generated build files should be excluded from my submission?
- What should I do if line endings or path separators cause platform-specific failures?
- How do I create a debug build with useful symbols and minimal optimization?
- How do I set a breakpoint, inspect a variable, and step into a function in the supported debugger?
- How do I use a backtrace to locate the origin of a segmentation fault?
- What should I do when a failure disappears while running under the debugger?
- Which sanitizer checks are expected, and what is the closest supported equivalent if ASan or UBSan is unavailable?
- How should I interpret the first relevant error in a sanitizer report rather than its later symptoms?
- What build command and tool output should I preserve as reproducible submission evidence?

## C Syntax, Types, Arrays, and Structures

- How do I read a declaration when `*` appears next to a type or variable name?
- What is the difference among `.`, `->`, `&`, and `*` in course code?
- When should I use a `struct` tag, a `typedef`, or both?
- How can a self-referential `struct` contain a pointer to its own type?
- What is the difference between a structure value and a pointer to a structure?
- When does an array expression decay to a pointer, and when does it not?
- Why does `sizeof(array)` differ from `sizeof(pointer)` after an array is passed to a function?
- Why is `sizeof *ptr` often safer than repeating a type name in an allocation?
- When is pointer arithmetic valid for an array, and when is it undefined?
- Why does the course use `size_t` for many sizes and indexes?
- What problems can occur when comparing signed and unsigned integers?
- When should I use fixed-width types such as `uint32_t` or `int64_t`?
- What does `const` protect in declarations such as `const TreeNode *`?
- Why are explicit state values useful instead of reserving a special key or data value?
- What belongs in a header file, and what belongs in a `.c` implementation file?
- How do include guards and linkage rules prevent duplicate declarations or multiple-definition errors?

## Pointers, Dynamic Allocation, and Ownership

- What exactly does a pointer store?
- How is `NULL` different from an uninitialized, dangling, or one-past-the-end pointer?
- How do I draw the difference between an object, its address, and a pointer variable that stores that address?
- When should I use `malloc`, `calloc`, or `realloc`?
- How do I calculate an allocation size without mixing element counts and byte counts?
- Why must every allocation result be checked before dereferencing it?
- Why should a `realloc` result be assigned to a temporary pointer first?
- Which existing pointers can become stale after storage is resized or moved?
- What does ownership mean in C when the language does not enforce it?
- How should an API document whether ownership is borrowed, transferred, shared, or retained?
- Who is responsible for freeing memory returned through an output parameter?
- When is it correct to free an individual node, and when is the node part of a non-owning arena?
- Why is reading a link field after freeing its node a use-after-free?
- How can deleting a linked node create a leak, dangling pointer, or lost remainder of the chain?
- How should partial construction be cleaned up after a later allocation fails?
- Is setting a pointer to `NULL` after `free` sufficient to make the program memory-safe?
- How can I verify that every successful allocation has exactly one valid cleanup path?
- How do aliases or multiple references change an otherwise simple ownership plan?

## Errors, Status Contracts, State Preservation, Overflow, and Recursion

- How should a C API distinguish success, an empty result, an invalid argument, an allocation failure, and a missing key?
- When should a function return a status and write the result through an output parameter?
- Should a reusable library function print its own error message or return a status so the caller controls the output?
- Must an output parameter remain unchanged when a function reports failure?
- What state is the caller allowed to observe after an operation fails?
- Which operations promise complete state preservation on failure?
- How do I test that an allocation failure leaves the old data, size, capacity, and links unchanged?
- Why should mutation sometimes be divided into prepare, validate, and commit phases?
- When may an operation make a partial change before reporting failure?
- How should an empty operation differ from malformed internal state?
- Why must external indexes and identifiers be checked before array access?
- How do I check multiplication or addition before computing an allocation size?
- How do I prevent capacity-growth arithmetic from wrapping around?
- How should weighted-path and total-cost addition be guarded against integer overflow?
- Why can subtracting two values in a sorting comparator overflow even when both values fit their type?
- How should infinity or an unreachable sentinel be chosen without making valid addition unsafe?
- What is the base case of a recursive function, and what happens if it is missing or unreachable?
- What state does each recursive call preserve on the runtime call stack?
- How do tree height and input shape affect recursion depth and stack-exhaustion risk?
- When should a recursive algorithm be converted to an explicit Stack or an iterative two-pass cleanup?

## Tests, Invariants, and Evidence

- What is the difference between an example test, a boundary test, a property test, and a regression test?
- Which empty, singleton, boundary, duplicate, malformed, and adversarial cases should every structure consider?
- How many nonduplicate student-authored tests are required for a given artifact?
- What makes two tests meaningfully different rather than the same case with new values?
- How should I test a failure path that is difficult to trigger naturally?
- What is a deterministic allocator hook, and how should its result be recorded?
- When is randomized differential testing appropriate, and what serves as the reference oracle?
- How should I preserve and report a random seed for a reproducible failure?
- Why should one failing test be minimized before I diagnose it?
- What is a representation invariant, and when should it be checked?
- How do preconditions, postconditions, and representation invariants differ?
- Can an invariant checker itself change the claimed complexity of an operation?
- What should I do if the output is correct but an invariant checker fails?
- How do I test destruction and cleanup without relying only on visible output?
- What counts as acceptable evidence: terminal text, test logs, sanitizer output, traces, diagrams, or screenshots?
- How should I annotate a Segfault Autopsy so it identifies the root cause rather than only the crash site?
- What makes a reconstructed path, traversal, or selected-edge set an independently checkable output certificate?
- How can I tell whether a failing instructor test reveals a contract misunderstanding or only an output-format mismatch?

## Complexity, Cost Models, and Performance Claims

- What do `O`, `Theta`, and `Omega` mean, and which one is being requested?
- What input variable does `n` represent for the operation I am analyzing?
- When should I use `V` and `E` instead of a single `n` for a graph algorithm?
- What do height `h`, width `w`, capacity `m`, and component count `c` measure?
- What is the difference between worst-case, expected, average-case, and amortized cost?
- Which assumptions must be stated before claiming expected constant time?
- Why can an operation be amortized constant time even though one call is linear?
- How do I include auxiliary space, recursion depth, or frontier size in an analysis?
- Why can the same algorithm have different bounds for a matrix and an adjacency-list representation?
- Should invariant checking and diagnostic instrumentation be included in the advertised operation bound?
- How do measured comparison counts support, but not prove, an asymptotic claim?
- When do constants, memory locality, and realistic workload mix matter despite identical Big-O notation?
- Why must I analyze the implementation strategy I actually used rather than quoting a textbook bound for a different strategy?
- How should I explain a worst-case degradation that depends on adversarial input ordering or collision patterns?

## Cybersecurity Context, Authorization, and Data Boundaries

- Why does the course use synthetic, instructor-controlled cybersecurity datasets?
- May I run course programs against a live network, filesystem, account system, or security tool?
- What counts as explicit authorization for testing data or systems outside the supplied environment?
- May I import real organizational logs, indicators, hostnames, or credentials into a lab?
- How should sensitive or personally identifying information be removed from test data and evidence?
- What should I do if a supplied or accidental input appears to contain real secrets or live-system data?
- Does an edge in a policy or communication graph prove that a route is exploitable?
- Does graph reachability establish authorization, compromise, or real-world risk?
- Does shared Union-Find membership imply trust between systems?
- Does a hash-table hash authenticate data or provide cryptographic collision resistance?
- When is collision flooding accurately described as algorithmic-complexity denial of service?
- How is algorithmic-complexity degradation different from memory corruption and general resource exhaustion?
- What assumptions make a synthetic edge weight meaningful as latency, cost, or risk?
- Why can a minimum-cost network or MST still be operationally fragile?
- When should a security recommendation add redundancy even if the algorithm's mathematical objective is already satisfied?
- May I create exploit-like malformed inputs for an authorized local test, and what resource limits must I use?
- How should I report a security-relevant defect found in course scaffolding or another student's code?
- Which conclusions must be qualified so a classroom model is not mistaken for a real penetration-test or risk assessment?
