# Module 2 — Stage B: Representation and Cognitive Pause

Open `representation_reveal.md` only when directed. It reveals how the
program stores the hierarchy and explains each new term before its code
example.

Then complete the three timed targets in `cognitive_pause.md` before expert
calibration—comparison with the instructor's model. After preserving your
initial answers, use `vocabulary.md` as a reference. An approved extended-time
or asynchronous response uses the same reasoning target. Speed and drawing
quality are not assessed.
