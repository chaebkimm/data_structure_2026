# Module 13 - Stage B: Representation and Cognitive Pause

Open `representation_reveal.md` only when directed. It names the hash table
and explains maps, the teaching hash, open addressing, linear probing,
`EMPTY`, `OCCUPIED`, and `DELETED` one term at a time.

Complete exactly the three targets in `cognitive_pause.md` before
calibration, which means comparison with the reference model. Keep
`vocabulary.md` closed until that response is preserved. An approved
extended-time or separately scheduled response uses the same examples and
targets. Speed and drawing quality are not assessed.
