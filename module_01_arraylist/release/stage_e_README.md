# Module 1 — Stage E: Lab and Evidence

Start with `student/lab.md`, then work only in:

- `code/starter/int_list.c`;
- `code/tests/test_student.c`.

The supplied core and extension tests are instructor-authored evidence; do not
edit them merely to make a failure disappear. The isolated autopsy is
deliberately faulty and is never part of the passing test suite.

Submit the artifacts listed in the lab and complete
`student/evidence_template.md`.
