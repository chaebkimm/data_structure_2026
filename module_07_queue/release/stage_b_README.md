# Module 7 — Stage B: Queue Representation and Cognitive Pause

Open `representation_reveal.md` only when the instructor tells you. It names
the arrival-order rule and explains circular storage one term at a time.

Then complete exactly the three timed targets in `cognitive_pause.md` before
calibration—comparison with the instructor model. Keep `vocabulary.md`
closed until the response is preserved. An approved extended-time or
separately scheduled response uses the same targets. Speed and drawing
quality are not assessed.
