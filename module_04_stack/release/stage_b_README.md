# Module 4 — Stage B: Fixed Stack Representation and Pause

Open `representation_reveal.md` only when the instructor tells you. It names
the last-in, first-out rule and shows a caller-owned fixed integer array with
separate size and capacity.

Then complete exactly the three targets in `cognitive_pause.md` before
calibration. Preserve the 100, 200, 300 trace, boundary decisions, output
values, and `1+2*3` explanation. Open `vocabulary.md` only after preserving
that response. An approved extended-time or separately scheduled response
uses the same targets; speed and drawing quality are not assessed.
