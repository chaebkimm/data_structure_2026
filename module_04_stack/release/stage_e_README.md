# Module 4 — Stage E: Lab and Evidence

Start with `student/lab.md`, then edit only:

- `code/starter/char_stack.c`;
- `code/starter/delimiter_validator.c`;
- `code/tests/test_student.c`.

The supplied tests and partially completed delimiter-validator code establish
the required behavior. The autopsy—a careful investigation after a
failure—contains an intentional defect and remains separate from passing
tests.

Submit the files and evidence listed in the lab and complete
`student/evidence_template.md`.
